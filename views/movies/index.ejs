<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movies List</title>
</head>
<body>
    <h1>My Movie Collection</h1>
    <a href="/users/<%= user._id %>/movies/new">Add New Movies</a>

    <ul>
        <% movies.forEach(movie => { %>
            <li>
                <a href="/users/<%= user._id %>/movies/<%= movie._id %>"><%= movie.title %></a>
            </li>
        <%})%>
    </ul>
</body>
</html>